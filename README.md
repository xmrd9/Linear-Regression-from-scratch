The code applies Linear Regression from scratch in Python.
It predicts the medical charges based on its relationship with BMI (Body Mass Index)
One of them applies Closed-Form while other uses Gradient Descent approach
